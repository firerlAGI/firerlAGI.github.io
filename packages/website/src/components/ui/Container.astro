---
interface Props {
  class?: string
}

const { class: className = '' } = Astro.props
---

<div class={`w-full max-w-[var(--container-width)] mx-auto px-5 md:px-8 lg:px-10 ${className}`}>
  <slot />
</div>
